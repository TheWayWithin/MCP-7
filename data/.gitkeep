# Data Directory

This directory stores the SQLite database and other data files generated by the MCP-7 Discovery Engine.

Files that will be created here:
- `mcp-discovery.db` - Main SQLite database
- `mcp-discovery-export-*.json` - Exported data files
- `discovery-logs-*.log` - Discovery run logs (if enabled)

The database will be automatically created when you first run the discovery engine.