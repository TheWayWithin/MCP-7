# OS Files
.DS_Store
Thumbs.db

# Environment Variables
.env
.env.local
.env.mcp

# agent-11 install backups. Five dated snapshots from 2025 sit here, each a full
# copy of the agents/missions/templates of the day. They are install artefacts,
# not work, and committing them would bloat the repo permanently. install.sh
# writes backups outside the repo as of 2026-08-05; these historic ones are safe
# to delete once checked.
.claude/backups/
.claude/*.backup-*
.claude/settings.json.backup-*

# MCP: keep the templates, ignore the resolved config and the generated status
# report. .mcp.json currently holds only ${VAR} interpolations, but it is the
# file that would hold real values if anyone ever inlined one, and .mcp-status.md
# is machine state written by mcp-setup.sh, not source.
.mcp.json
.mcp-status.md

# SQLite runtime databases. data/mcp-discovery.db is written by the discovery
# engine; the -wal and -shm sidecars are only meaningful beside the exact db
# that produced them, so committing the set captures one machine's
# mid-transaction state and conflicts on every pull.
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
logs/

# Dependencies
node_modules/
venv/
__pycache__/

# Build outputs
dist/
build/
*.pyc

# Temporary files
*.tmp
.cache/

# Personal notes
notes.md
TODO.md
scratch/